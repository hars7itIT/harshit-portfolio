# Get a key from https://console.anthropic.com/ and paste it below,
# then rename this file to .env.local (never commit the real key).
ANTHROPIC_API_KEY=

# Get a key from https://resend.com/ to make the contact form actually send
# email instead of falling back to opening the visitor's mail client.
RESEND_API_KEY=
